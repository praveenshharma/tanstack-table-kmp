# Gradle
.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar
local.properties

# IntelliJ / Android Studio
.idea/
*.iml
*.iws
out/
.kotlin/

# Kotlin/Native
.konan/

# Xcode / iOS
xcuserdata/
*.xcuserstate
*.xcworkspace/xcuserdata/
DerivedData/

# OS files
.DS_Store
Thumbs.db

# Logs and artifacts
*.log
*.hprof
captures/

# Local secrets (Maven Central publishing)
secrets/
*.gpg
*.asc
gradle.properties.local

# Test output
test-results/
test-output/

# Claude Code local-only settings (not for the repo)
.claude/settings.local.json

# Generated by Android Studio's updateDaemonJvm task — pins the Gradle daemon
# to whichever JDK Studio bundles (currently JetBrains Runtime), which doesn't
# exist on CI. Each developer's local copy is fine; the repo doesn't need it.
gradle/gradle-daemon-jvm.properties
