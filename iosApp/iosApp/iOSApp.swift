import SwiftUI

// SwiftUI entry point for the iOS sample. Renders the shared Compose UI exposed
// by the SampleApp framework (see ContentView).
@main
struct iOSApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}
